package io.macrosquare.integrity.domain;

/** Stable metric keys for recurrence checks backed by independently persisted evidence. */
public enum IntegrityMetric {
    COMPANY_UNIVERSE_ROWS,
    COMPANY_CURRENT_CALCULATION_ROWS,
    COMPANY_COMPARABLE_SCORE_ROWS,
    COMPANY_PRICE_SIGNAL_ROWS,
    INVALID_COMPANY_SCORE_ROWS,
    INCOMPLETE_COMPANY_SCORE_ROWS,
    FUTURE_COMPANY_DATE_ROWS,
    NONCURRENT_SCORED_ROWS,
    BUY_WITHOUT_EVIDENCE_ROWS,
    INCOMPLETE_PRICE_SIGNAL_ROWS,
    UNAVAILABLE_COMPANY_ROWS,
    RETIRED_OR_ALIAS_COMPANY_ROWS,
    CANONICAL_MRSH_ROWS,
    MARKET_COLLECTION_STATUS_ROWS,
    SECTOR_PRICE_SERIES_ROWS,
    SECTOR_HISTORY_READY_ROWS,
    SECTOR_BENCHMARK_READY_ROWS,
    HARD_COLLECTION_FAILURE_ROWS,
    STALE_COLLECTION_ROWS,
    STALE_SECTOR_PRICE_ROWS,
    MISALIGNED_SECTOR_PRICE_ROWS,
    SECTOR_PRICE_DISCONTINUITY_ROWS,
    SECTOR_TOTAL_RETURN_SERIES_ROWS,
    SECTOR_TOTAL_RETURN_HISTORY_READY_ROWS,
    SECTOR_TOTAL_RETURN_BENCHMARK_READY_ROWS,
    CURRENT_SECTOR_ROTATION_READY_ROWS,
    INVALID_SECTOR_ROTATION_RUN_ROWS,
    STALE_SECTOR_TOTAL_RETURN_ROWS,
    MISALIGNED_SECTOR_TOTAL_RETURN_ROWS,
    SECTOR_TOTAL_RETURN_DISCONTINUITY_ROWS,
    FUTURE_MARKET_ROWS,
    NONFINITE_MARKET_ROWS,
    DUPLICATE_MARKET_ROWS,
    FUTURE_ANALYST_ROWS,
    INVALID_ANALYST_ROWS,
    DUPLICATE_ANALYST_ROWS,
    ANALYST_SERIES_ROWS,
    STALE_ANALYST_SERIES_ROWS,
    EMPTY_LATEST_ANALYST_ROWS,
    INVALID_13F_HOLDING_ROWS,
    INVALID_13F_DATE_ROWS,
    SUSPICIOUS_13F_UNIT_GROUPS,
    DANGLING_OBJECT_POINTER_ROWS,
    CANDIDATE_DRIFT_ROWS,
    OUTBOX_RETRY_ROWS,
    OUTBOX_DEAD_ROWS,
    OUTBOX_STUCK_ROWS
}
